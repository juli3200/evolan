use std::os::raw::c_char;
/*
extern "C"{
    fn calculate(a: i32);
}
*/
// calculates every output neuron of every bot
pub fn calc_step(input_neurons: &Vec<Vec<[f64; 5]>>){

}

pub fn calc_step_gpu(input_neurons: &Vec<Vec<[f64; 5]>>){

}